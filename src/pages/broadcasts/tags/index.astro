---
import { getCollection } from "astro:content";
import BaseLayout from "../../../layouts/BaseLayout.astro";
import { isPublished } from "../../../lib/content";

const all = await getCollection("broadcasts", isPublished);

const counts = new Map<string, number>();
for (const entry of all) {
  for (const tag of entry.data.tags) {
    counts.set(tag, (counts.get(tag) ?? 0) + 1);
  }
}
const tags = Array.from(counts.entries())
  .map(([tag, count]) => ({ tag, count }))
  .sort((a, b) => b.count - a.count || a.tag.localeCompare(b.tag));
---
<BaseLayout
  title="Exosphere — Broadcast tags"
  description="Browse Broadcasts by topic."
  currentPath="/broadcasts"
>
  <section class="page">
    <div class="page__headline">
      <h6>Broadcasts · Tags</h6>
      <h1>Browse by topic.</h1>
      <p>Every topic in the archive, most written about first.</p>
    </div>

    <div class="page__body">
      {tags.length === 0 ? (
        <p class="list__empty">No tags yet.</p>
      ) : (
        <ul class="links">
          {tags.map(({ tag, count }) => (
            <li>
              <a href={`/broadcasts/tags/${tag}`}>{tag}</a> <small>{count}</small>
            </li>
          ))}
        </ul>
      )}

      <ul class="links">
        <li><a href="/broadcasts">All broadcasts</a></li>
      </ul>
    </div>
  </section>
</BaseLayout>
