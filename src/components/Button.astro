---
interface Props {
  href: string;
  variant?: "primary" | "ghost";
  arrow?: boolean;
}
const { href, variant = "ghost", arrow = false } = Astro.props;
const cls = `btn btn--${variant}`;
---
<a class={cls} href={href}>
  <slot />
  {arrow && <span class="arrow">→</span>}
</a>
